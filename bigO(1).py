def func_constant(values):
    """
    Prints first Item in a list of values
    """
    print(values[0])

func_constant([1,2,3,4])
# it does not matter how large the numbers in value list is, the function/algo will just grab the indexed position 0 in the list.so this is a O(n) operation a constant operation